# QuietHarness v3 launch visuals

Four Chinese 16:9 cards for the QuietHarness launch story:

1. `01-positioning.zh` — prompt length is a symptom; misplaced runtime layers are the problem.
2. `02-three-layers.zh` — always-loaded core, on-demand capabilities, and background systems.
3. `03-before-after.zh` — the old ritual-heavy day versus the short interactive path.
4. `04-rule-placement.zh` — a reusable decision tree plus the reversible migration path.

The SVG files are editable sources. The PNG files are rendered copies intended for X. No image claims response speed or model-quality improvement; byte counts and implementation evidence live in the repository README and release review.

`X-DRAFT.zh.md` contains the unpublished main post and two optional replies. Publishing remains a separate maintainer gate.
