# 私人工作上下文示例

- 默认语言：中文。
- 业务线注册表：`<private-business-line-registry>`。
- 任务事实源：`<private-per-task-store>`。
- 进入一个工作区时，只读取该业务线的有限摘要。
- 明确点名任务时，只读取对应任务并报告来源与更新时间。
- 状态缺失、矛盾或过期时，报告 `UNKNOWN` 并回源核验。
- 状态发生持久变化时，通过 Owner writeback 保存。
- 当前优先级、任务内容、项目事实、账号、凭证和客户数据不放进这个常驻文件。
