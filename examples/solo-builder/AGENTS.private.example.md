# Private working context — example

- Preferred language: `<language>`.
- Stable working preference: `<one preference that applies across projects>`.
- Workspace registry: `<private workspace registry path>`.
- Task readout: `<command or adapter for one named task>`.
- Task writeback: `<command or adapter that updates the canonical task record>`.
- When state is missing, contradictory, or stale, report `UNKNOWN` and verify the source.
- Keep current priorities, task lists, project facts, credentials, and account data out of this always-loaded file.
